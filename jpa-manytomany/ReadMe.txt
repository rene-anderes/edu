Beispiel für JPA und Many-To-Many Beziehung (unidirektional)

Test läuft mittels Derby-Datanbank
URL: jdbc:derby://localhost:1527/myEmployee;create=true

Derby starten: [JDK Installation]\db\bin\startNetworkServer.bat
