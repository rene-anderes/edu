CREATE TABLE AUTHOR (ID BIGINT NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), VITA VARCHAR(255), PRIMARY KEY (ID));
CREATE TABLE BOOK (ID BIGINT NOT NULL, DESCRIPTION VARCHAR(255), ISBN VARCHAR(255), TITLE VARCHAR(255), PUBLISHER_ID BIGINT, PRIMARY KEY (ID));
CREATE TABLE PUBLISHER (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID));
CREATE TABLE PHONEBOOK (ID BIGINT NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PHONENUMBER VARCHAR(255), PRIMARY KEY (ID));
CREATE TABLE AUTHOR_BOOK (AUTHOR_ID BIGINT NOT NULL, BOOK_ID BIGINT NOT NULL, PRIMARY KEY (AUTHOR_ID, BOOK_ID));
ALTER TABLE BOOK ADD CONSTRAINT BOOK_PUBLISHER_ID FOREIGN KEY (PUBLISHER_ID) REFERENCES PUBLISHER (ID);
ALTER TABLE AUTHOR_BOOK ADD CONSTRAINT AUTHOR_BOOKBOOK_ID FOREIGN KEY (BOOK_ID) REFERENCES BOOK (ID);
ALTER TABLE AUTHOR_BOOK ADD CONSTRAINT AUTHORBOOKAUTHORID FOREIGN KEY (AUTHOR_ID) REFERENCES AUTHOR (ID);
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME));
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0);
